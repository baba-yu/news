# Claude Code
CLAUDE.md
.claude/
.cursorrules
.cursor/
.vscode/
*.code-workspace

# Other agents
AGENTS.md

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
*.swp
*.swo
*~

# tmp
*.tmp
.bindfs-trash/
.tmp/

# Ref
references.txt
design/

# Pipeline byproducts that change every run; sourcedata is tracked
# but the SQLite DB + reproducible exports are not.
app/data/
app/__pycache__/
app/**/__pycache__/
app/.pytest_cache/
app/tests/__pycache__/

# Aged-out dashboard snapshots — moved here by app/skills/archive_snapshots.py.
# Kept locally for reference, never published to Pages (keeps the tar(./docs)
# artifact small enough for GitHub Pages to sync).
docs/archives/
