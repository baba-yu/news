# Theme review — week ending 2026-05-05

Source data: docs/data/graph-{tech,business,mix}.json (post-2026-05-04 daily rebuild) + theme_candidates pending rows + app/src/schema.sql. Run mode: routine Sunday rotation, executed on Tuesday 2026-05-05 as a test run with `apply_mode: manual` on every recommendation (no auto schema edit). Cluster shape since the 2026-05-03 review with the 5/3 description-widening recommendations now applied: tech.agent_registry_architecture grew 1 to 17, business.open_weight_vs_proprietary grew 1 to 14, business.developer_platformization grew 2 to 14, tech.model_supply_chain grew 2 to 9.
Total themes: tech 8, business 6, mix 14. Total predictions: tech 39, business 37, mix 55 unique. Pending theme_candidates: 34 rows (all `no_keyword_match`, none with `nearest_theme_id` set).

## Empty / underused themes

### tech
- **`AI Chip Architecture`** (`tech.ai_chip_architecture`) — 2 children. Stable shape, but the description is the narrowest in the schema (no Cerebras, no AMD, no per-token-margin SKU split, no AI-accelerator-revenue 10-Q segment language). The Cerebras IPO mid-May print, the AMD AI-accelerator-revenue 10-Q segment cluster (3 pending rows mention it), and Mag-7 training-vs-inference SKU split are all on-theme but currently absorbed by `business.hyperscaler_frontier_lab_alliance` and `business.compute_capex_strategy` via the secondary-attach path.
  - Suggested: rewrite description to widen the keyword surface. Add **Cerebras IPO, WSE-3, AMD MI355X, AMD AI-accelerator revenue, training-vs-inference SKU split, accelerator-revenue 10-Q segment footnote, GB300 vs Trainium positioning, per-accelerator margin disclosure, hyperscaler custom silicon** terminology. The May 5 cycle has enough chip-architecture signal for this theme to absorb 4 to 6 of next week's predictions if rephrased.

### business
- (no underused themes — all 6 carry ≥ 9 children after the 5/3 description-widening landed.)

### mix
- (rolls up tech + business; no mix-specific empties.)

## Overpopulated themes

The 5/3 review's diagnosis carries forward and got worse: a small set of "broad-context" predictions multi-attach across many themes via the IDF secondary-attach path in `app/src/export.py`. Today the most-attached predictions are *Microsoft Agent 365 GA forces Big-3 MCP policy*, *Capital markets reprice agent-displaced SaaS by agent-touched API share*, *AMD discloses AI-accelerator revenue as 10-Q segment footnote*, *OCC publishes agentic-AI Model Risk Management addendum*, *Frontier cyber-AI export-control category emerges*. None are theme-specific and they each saturate every theme they share even one rare token with.

### tech
- **`Agent Runtime Security`** (21 children, +5 from 5/3's 16) — on-topic for ~10 children (Indirect prompt injection, Agent-in-the-Loop secret exfiltration, MCP protocol redesign, Inference servers form supply chain, AI-Infra CVE class, Agent-skills attack surface, CISA AI-Infra KEV, Agentic-AI CVE class, LLM-tooling + Linux-kernel CVE chain, Frontier cyber-AI export-control). The remaining 11 are multi-attach (incl. AMD 10-Q footnote, Capital-markets reprice, Hyperscaler JV ethics carveouts).
  - Suggested: leave as-is. The on-topic cluster is coherent — it's the AI-security-substrate thesis arc. The multi-attach noise is an export.py threshold issue, not a schema issue.
- **`Agent Registry Architecture`** (17 children, +16 from 5/3's 1) — the 5/3 keyword-widening worked, perhaps too well. On-topic for ~7 (Headless Everything, Agent registry standard converges with MCP, Microsoft Agent 365 GA forces MCP policy, Cross-cloud agent-identity + MCP registry RFC, NIST non-human-identity control profile, Agent-skills threat sub-matrix, Agent-as-first-class-identity statute). Remaining 10 are multi-attach via the broad "Agent" / "MCP" / "registry" / "identity" tokens.
  - Suggested: leave description as-is (the gain is real); flag the multi-attach pattern for the same export.py investigation as `agent_runtime_security`.

### business
- **`Hyperscaler × Frontier Lab Alliance`** (29 children, +8 from 5/3's 21) — most populated theme in the corpus, up from 21 last review and 15 the review before. On-topic for ~10 (Anthropic-AWS pact 5-pole regime, Three-cloud training oligopoly, Hyperscaler-AI-lab capital coupling, Big-3 per-token-margin floor, Frontier-model SOC2/FedRAMP/FMF baseline, Multi-cloud SLA standardization, Cross-cloud agent-identity RFC, AGI-clause unwind, Foundation labs audited monthly revenue, Hyperscaler custom silicon training/inference SKU split). Remaining 19 are multi-attach.
  - Suggested: leave as-is. Splitting a healthy 10-child cluster to clean up 19 multi-attach rows is the wrong tradeoff.
- **`AI Security Compliance Market`** (18 children, +5 from 5/3's 13) — same multi-attach shape. On-topic for ~7 (Frontier-model SOC2 baseline, MCP protocol redesign as enterprise bottleneck, Agentic-AI CVE class CSAI CNA, OCC agentic-AI MRM addendum, Agent-in-the-Loop Secret Exfiltration, Frontier cyber-AI export-control, OpenAI audited monthly revenue cadence). Remaining 11 are multi-attach.
  - Suggested: leave as-is.
- **`Open Weight vs Proprietary AI`** (14 children, +13 from 5/3's 1) — 5/3 keyword-widening worked. On-topic for ~6 (Closed-weight frontier crystallizes as open weights cap at 35B-A3B, Open-weight 1M-context era arrives as new floor, Consumer-GPU coding agents displace cloud-only APIs for 30% of devs, KV-cache-compression dtype as inference-stack knob, Local-LLM stacks expose unified compressed-KV-cache flag, Unsloth 2026 stack as hyperscaler-managed fine-tuning service). Remaining 8 are multi-attach.
  - Suggested: leave as-is.
- **`Developer Toolchain Platformization`** (14 children, +12 from 5/3's 2) — 5/3 keyword-widening worked. On-topic for ~5 (Agent Control Plane as hyperscaler battleground, Capital markets reprice agent-displaced SaaS by API share, Consumer-GPU coding agents displace cloud-only APIs, Multi-cloud SLA standardization wave, Unsloth 2026 stack as managed fine-tuning service). Remaining 9 are multi-attach.
  - Suggested: leave as-is.
- **`Compute Capex Strategy`** (12 children) — first appearance over the 6-threshold. On-topic for ~6 (Hyperscaler training/inference SKU split, Big-3 per-token-margin floor, Hyperscaler per-token-margin-gated GPU capex, AMD AI-accelerator revenue 10-Q footnote, Microsoft audited monthly KPIs as 10-Q footnote, KV-cache-compression dtype as inference-stack knob). Remaining 6 are multi-attach.
  - Suggested: leave as-is.

## Theme candidates

`theme_candidates` holds 34 pending rows (was 0 on 5/3 — the post-update-validation pass that landed during the week is now writing rows). All 34 are `no_keyword_match` (no `nearest_theme_id` proposed). Manual clustering by topic surface:

| scope | suggested label | hits | proposed category | sample evidence |
|---|---|---|---|---|
| tech | AI Macro & Capital-Markets Reset | 10 | tech.infrastructure or new tech.macro | Mag 7 Q1 earnings reset AI-capex ROI; OpenAI Apr 28 WSJ + Apr 29 Mag 7 print collision triggers SEC AI-revenue disclosure rewrite; Powell Apr 29 Board-of-Governors decision shifts 2026 monetary-policy market psychology; Cerebras IPO mid-May ≥$22-25B; Apple +$100B buyback / 4% dividend hike + $700B-2026-AI-capex print splits AI-capex disclosure into two reporting cadences |
| tech | Frontier-Lab × Cloud Capacity Capture | 6 | tech.standards | Exclusive alliance hyperscaler-frontier-lab era closes; Tier-1 cloud capacity capture splits field by Q3 2026; Hyperscaler-AI-lab capital coupling graduates to compute-stack lock-in; AGI-clause unwind sets template; OpenAI-on-AWS-Bedrock + Microsoft-license-non-exclusive triggers multi-cloud SLA wave; Two more frontier labs publish autonomous-weapons carve-outs |
| business | Inference-Server Supply-Chain Risk | 7 | business.regulation-compliance | Inference servers as primitive supply chain; Indirect prompt injection top CVE category; GGUF supply chains; OAuth trust between AI SaaS top enterprise risk; AI-Infra CVE class graduates to regulatory primitive; Inference-server SSTI to OWASP LLM Top-10 v2026; Agent-skills attack surface threat sub-matrix |
| business | Physical AI Industrial Procurement | 3 | business.enterprise-adoption | Physical AI as SaaS / Robot-as-a-Service infrastructure race; Physical AI 8-hour production runs as enterprise procurement requirement; Physical AI production-line records become league tables at IROS / GTC 2026 |
| business | Agent Skills Marketplace Governance | 3 | business.regulation-compliance | Agent-skills attack surface threat sub-matrix lands as MITRE ATT&CK extension or NIST AI-RMF appendix; Agent-skills marketplace ships enforced signing + provenance gate; NIST publishes non-human-identity control profile |
| (mixed) | Open-Weight Pricing Floor & Edge LLM | 3 | tech.models | 1-bit native training becomes dominant alternative to post-hoc quantization; 27B Dense + 1M context becomes de facto standard for enterprise local LLMs; Acceleration of local-first cloud-overflow inversion |

Of these clusters, the strongest signal-to-noise candidates for new themes are the macro/capital-markets cluster (10 hits, no existing theme covers it cleanly) and the inference-server-supply-chain cluster (7 hits, already absorbed by `tech.agent_runtime_security` but the candidates landed in `business` scope where there's no clean theme).

The other clusters either fit existing themes after a description tweak (Frontier-Lab × Cloud Capacity → `business.hyperscaler_frontier_lab_alliance` already covers it; Open-Weight Pricing Floor → `business.open_weight_vs_proprietary` and `tech.one_bit_edge_llm` already cover it) or are below the conservative ≥ 3 distinct-day threshold for promoting a new theme on first appearance.

## Category-level notes

- `tech.security` carries 30/39 = 76.9% of tech-scope predictions (was 18/49 = 37% on 5/3). This crosses the 50% dominance threshold — a single category is now absorbing three-quarters of the tech scope. Two themes attach there: `tech.agent_runtime_security` (21) + `tech.model_supply_chain` (9). The dominance is real (the 2026-05-week cycle is unusually security-heavy), but it also reflects the multi-attach saturation in `agent_runtime_security`. Worth carving the security category into `tech.security` (runtime / CVE / inference-server) vs. a new `tech.agent-governance` (registry / identity / standards) bucket — but that is a category-level edit and per `design/memory-policy.md` §2.3 categories are even more stable than themes; defer to a separate design discussion.
- `business.market-structure` carries 29/37 = 78.4% of business-scope predictions (was 21/49 = 43% on 5/3). Same shape as tech.security: a single category absorbing the bulk of the scope. Driven entirely by `business.hyperscaler_frontier_lab_alliance` (29 children, single theme = full category). Same conclusion: real concentration plus multi-attach, defer category-level edits.
- `business.regulation-compliance` carries 18/37 = 48.6% of business-scope predictions — just under the 50% dominance threshold but worth flagging for next week's review.
- (No category sat dormant for ≥ 2 consecutive reviews.)

## Recommended actions

1. **Tighten description on `tech.ai_chip_architecture`** (`type: rename`/`apply_mode: manual`). Add Cerebras IPO / WSE-3 / AMD MI355X / AMD AI-accelerator revenue / training-vs-inference SKU split / accelerator-revenue 10-Q segment footnote / GB300-vs-Trainium positioning / per-accelerator margin disclosure / hyperscaler custom silicon terminology. This is the only currently-underused theme and the 5/5 cycle has 4 to 6 chip-architecture signals (AMD 10-Q, Cerebras IPO, Mag-7 training/inference SKU split) it should be absorbing. Test-mode proposal — do not auto-apply.
2. **Add new theme `tech.ai_macro_capital_markets`** (`type: new`/`apply_mode: manual`). Category: `tech.infrastructure` (closest existing bucket; a new `tech.macro` category is a separate design discussion). Description draft: "Macro and capital-markets dynamics shaping AI: Mag 7 super-week earnings, AI-capex ROI repricing, AI-revenue disclosure rewrite (SEC concept release, OpenAI audited revenue cadence, AMD AI-accelerator 10-Q segment), Powell-Fed Board institutional-volatility regime, Cerebras IPO, FOMC dissent splits, Apple-buyback collision with $700B AI-capex print, AI-capex disclosure splits into two reporting cadences." Justification: the 10-hit cluster in `theme_candidates` is the largest single no-match cluster and no existing theme cleanly absorbs it. Test-mode proposal — do not auto-apply.
3. **Add new theme `business.inference_server_supply_chain`** (`type: new`/`apply_mode: manual`). Category: `business.regulation-compliance`. Description draft: "Inference-server supply-chain governance: AI-Infra CVE class as regulatory primitive, indirect prompt injection as top CVE category, GGUF supply-chain integrity, OAuth trust between AI SaaS, inference-server SSTI to OWASP LLM Top-10 v2026, agent-skills attack-surface threat sub-matrix, CISA AI-Infra KEV sub-catalog." Justification: 7 pending business-scope rows describe inference-server / supply-chain risk in market/compliance terms, but the only existing security theme is `tech.agent_runtime_security` which is tech-scope. Adds a business-scope counterpart so the multi-attach saturation on `business.ai_security_compliance_market` (18 children) thins. Test-mode proposal — do not auto-apply.
4. **Investigation (no schema edit): `SECONDARY_THEME_THRESHOLD` in `app/src/export.py`** — third consecutive review flagging the same multi-attach pattern (themes saturating from broad-context predictions sharing one rare token). Pattern is now wider than 5/3: 29 / 21 / 18 / 17 / 14 / 14 / 12. Tightening the threshold (or requiring a non-generic token in the secondary-attach overlap) would clean the overpopulation appearance without any schema edit and would also unmask more genuine no-match candidates for `theme_candidates`. Defer to a separate engineering session — explicitly out of scope for this Sunday's `apply-schema-edit` flow. Already noted as a carry-forward on 5/3 and 4/27.
5. **Carry-forward investigation (no schema edit): category-level dominance in `tech.security` (76.9%) and `business.market-structure` (78.4%)**. Both crossed the 50% dominance threshold this week. Driven by single-theme concentration (`agent_runtime_security` 21/30 = 70% of tech.security; `hyperscaler_frontier_lab_alliance` 29/29 = 100% of business.market-structure) plus the same secondary-attach saturation flagged in action 4. A category-level carve-out (e.g. splitting `tech.security` into runtime-CVE vs agent-governance buckets) would address the dominance, but per `design/memory-policy.md` §2.3 categories are more stable than themes and require their own design discussion. Surface the dominance signal here and revisit next week if it persists. No schema edit proposed.
