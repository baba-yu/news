# Theme review — week ending 2026-06-14

Mode: routine Sunday rotation (5_weekly_theme_review). Diagnostic + advisory pass over the current schema. Inputs: `docs/data/graph-{tech,business,mix}.json` (post-update_pages snapshot, generated 2026-06-15T01:24Z), `theme_candidates` table (read-only, 38 pending rows), `app/src/schema.sql`.

## Diagnostic summary

Total themes: tech 10, business 8, mix 18 (unchanged). Total predictions: tech 153, business 161, mix 176 unique. Pending `theme_candidates`: 38 rows (15 business-scope, 23 tech-scope), all `candidate_reason='no_keyword_match'`, none carrying a `nearest_theme_id`. Max within-scope hit count = 1 across all 38 labels (each label is a distinct one-off prediction string). Eighth consecutive week the `≥ 3 distinct-day` promotion bar cannot fire on raw labels (5/10, 5/17, 5/24, 5/31, 6/07, this week).

**The headline this week: `business.ai_revenue_disclosure` over-fit eased on the metrics, but recommendation 1 (sharpen its description) STILL never landed.** The most recent schema commit remains `e5e0a79` (5/24); neither the 5/31, the 6/07, nor any intervening `rewrite-description` block reached `app/src/schema.sql`. The theme's description at `app/src/schema.sql:1234` still carries every broad anchor token the sharpening was meant to drop ("Big-3", "foundation labs (OpenAI, Anthropic, xAI)", "Microsoft Azure", "Alphabet", "AMD", bare company names). Verified directly against the pre-review snapshots: the `Big-3` anchor is present in `20260531-pre-review`, `20260607-pre-review`, and `20260614-pre-review` schema copies alike — three straight weeks unchanged.

What *did* change is organic, not structural. Over the past week new predictions landed and the IDF tournament re-ran against the **unchanged** schema, and the result moved in the direction the sharpening was supposed to force: `business.ai_revenue_disclosure` primary kids fell **124 → 90**, `business.capital-supply-chain` category dominance fell **76.0 % → 70.2 %** (its first decline after four straight weeks of climbing, 52.2 % → 68.9 % → 74.0 % → 76.0 % → 70.2 %), and the previously-starved `business.compute_capex_strategy` jumped from **4 children / 3 primary to 67 children / 23 primary** as fresh compute-capex predictions matched it directly. This closes the 6/07 "deferred" watch on capex-strategy decisively — it is no longer underused and no longer a merge candidate. But the relief is data-driven and reversible: `business.ai_revenue_disclosure` at 90 primary is still by a wide margin the largest theme in any scope, the description is still the broadest of the 8 business themes, and the off-topic kids that re-routed this week can route straight back the next time a quiet news week leaves the IDF tournament with nothing closer to match. Recommendation 1 below re-emits the same vetted sharpening, unchanged, so the improvement is locked into the schema rather than left to weekly matcher noise.

### Tech scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| Model Supply Chain (`tech.model_supply_chain`) | tech.security | 101 | 48 |
| AI Macro & Capital Markets (`tech.ai_macro_capital_markets`) | tech.infrastructure | 62 | 34 |
| Agent Runtime Security (`tech.agent_runtime_security`) | tech.security | 43 | 12 |
| Agent Registry Architecture (`tech.agent_registry_architecture`) | tech.standards | 31 | 9 |
| Frontier Model Regulatory Board (`tech.frontier_model_regulatory_board`) | tech.standards | 29 | 16 |
| Agent Control Plane (`tech.agent_control_plane`) | tech.agents | 21 | 11 |
| Local Inference Runtime (`tech.local_inference_runtime`) | tech.inference-runtime | 16 | 10 |
| AI Chip Architecture (`tech.ai_chip_architecture`) | tech.infrastructure | 12 | 4 |
| 1-bit / Edge LLM (`tech.one_bit_edge_llm`) | tech.models | 11 | 3 |
| Physical AI / Robotics (`tech.physical_ai_robotics`) | tech.infrastructure | 8 | 6 |

Tech category density (primary attach via prediction `category_id`, 153 distinct predictions): tech.security 60 / 153 = 39.2 %, tech.infrastructure 44 / 153 = 28.8 %, tech.standards 25 / 153 = 16.3 %, tech.agents 11 / 153 = 7.2 %, tech.inference-runtime 10 / 153 = 6.5 %, tech.models 3 / 153 = 2.0 %. No category over 50 %. The top tech category flipped from infrastructure to security this week (model-supply-chain grew strongly to 101 / 48), but both sit comfortably below threshold and the FMRB carve-out continues to hold tech.standards balanced. Tech taxonomy remains healthy.

### Business scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| AI-Revenue Disclosure Rewrite (`business.ai_revenue_disclosure`) | business.capital-supply-chain | 141 | 90 |
| Inference Server Supply Chain (`business.inference_server_supply_chain`) | business.regulation-compliance | 80 | 24 |
| Compute Capex Strategy (`business.compute_capex_strategy`) | business.capital-supply-chain | 67 | 23 |
| Developer Toolchain Platformization (`business.developer_platformization`) | business.enterprise-adoption | 38 | 8 |
| Hyperscaler × Frontier Lab Alliance (`business.hyperscaler_frontier_lab_alliance`) | business.market-structure | 34 | 3 |
| Open Weight vs Proprietary AI (`business.open_weight_vs_proprietary`) | business.competition | 29 | 10 |
| AI Security Compliance Market (`business.ai_security_compliance_market`) | business.regulation-compliance | 20 | 2 |
| Cloud vs Local AI Distribution (`business.cloud_vs_local_distribution`) | business.distribution | 19 | 1 |

Business category density (primary attach, 161 distinct predictions): **business.capital-supply-chain 113 / 161 = 70.2 %** (down from 76.0 % — first improvement in five weeks), business.regulation-compliance 26 / 161 = 16.1 %, business.competition 10 / 161 = 6.2 %, business.enterprise-adoption 8 / 161 = 5.0 %, business.market-structure 3 / 161 = 1.9 %, business.distribution 1 / 161 = 0.6 %. Single-category dominance on business eased this week but is still well over the 50 % flag line and remains the project's only category over threshold. Critically, the two capital-supply-chain themes now split the category's primary load far more evenly (90 + 23 vs the 124 + 3 of 6/07) — the capex-strategy revival absorbed predictions that previously over-concentrated on `business.ai_revenue_disclosure`. The structural fix (a new category to pull `business.ai_revenue_disclosure` out of `business.capital-supply-chain`) remains the durable answer; see recommendation 3.

### Mix scope

Rolls up tech + business; same theme + category shape as above, with primary re-tournamented across the combined 18-theme pool. Total unique predictions = 176. In the mix pool, several business themes shed nearly all their primary kids to better-matching tech themes — `business.ai_revenue_disclosure` drops to 13 primary (from 90 in business-only), `business.inference_server_supply_chain` and `business.ai_security_compliance_market` fall to 0 primary, `business.developer_platformization` / `business.open_weight_vs_proprietary` / `business.cloud_vs_local_distribution` to 1 each — confirming these clusters are tech-flavored predictions parked in the business pool only because no tech theme competes in business-only mode. Mix category density is well-distributed: top is tech.security at 34.1 %, no category over 50 %.

### Multi-attach pressure (secondary-attach path in `app/src/export.py`)

Distribution of theme-parents per prediction (counted via theme-node `child_ids` membership):

- Tech (153 predictions): 58 attach to exactly one theme, 43 to two, 27 to three, 19 to four, 4 to five, 1 to six, 1 to seven. 95 / 153 = 62.1 % multi-attach.
- Business (161 predictions): 46 to one, 42 to two, 29 to three, 21 to four, 13 to five, 8 to six, 2 to seven. 115 / 161 = 71.4 % multi-attach.
- Mix (176 predictions): 20 to one, 28 to two, 30 to three, 29 to four, 18 to five, 14 to six, 15 to seven, 8 to eight, 9 to nine, 2 to ten, 2 to eleven, 1 to fourteen. 156 / 176 = **88.6 % multi-attach**.

The same broad-context-prediction pattern flagged every week since 4/27 — ninth consecutive week. Mix multi-attach eased slightly to 88.6 % (from 90.1 %). This is an `export.py` secondary-attach-threshold property, not a schema fault; carried forward as an engineering item (recommendation 4).

## Empty / underused themes

No theme is empty (child/prediction count 0 or 1) on the primary `child_ids` measure. The two lowest-primary themes:

- **`business.cloud_vs_local_distribution` — 19 children, 1 primary.** Healthy child count (19) keeps it visible on the dashboard via secondary attach, but its primary core is the thinnest of any theme. It carves a genuinely distinct sub-space (on-device vs hosted distribution shifts, cloud-overflow inversion, SMB local-first) that does not overlap any other business theme's keyword set, so it is not a merge candidate — it simply wins few IDF tournaments because most distribution-flavored predictions also carry stronger capital / compliance signal. Log-only watch; flag if primary stays ≤ 1 next week.
- **`business.ai_security_compliance_market` — 20 children, 2 primary (0 primary in mix scope).** Unchanged posture from 6/07 (was 25 / 2). Healthy child count keeps it visible; its primary core stays thin because it competes against `business.inference_server_supply_chain` (same `business.regulation-compliance` category, 24 primary) for regulation/compliance predictions. Still not a merge candidate — the two themes carve genuinely different sub-spaces (compliance-market/spend vs supply-chain-CVE-governance) and the keyword sets are distinct. Log-only watch; flag if primary stays ≤ 2 next week.

The 6/07 underused watch — `business.compute_capex_strategy`, flat at 3 primary for three weeks — **resolved this week without intervention**: it jumped to 67 children / 23 primary as fresh compute-capex predictions matched it directly. The merge-into-`business.ai_revenue_disclosure` decision that was deferred to "one week after recommendation 1 lands" is now moot; capex-strategy is a healthy, well-populated theme. No action needed there.

## Overpopulated themes

Several themes exceed the ≥ 6-prediction threshold; all are coherent except the one persistent structural concern:

- **`business.ai_revenue_disclosure` — 141 children, 90 primary. PERSISTENT, STILL-UNRESOLVED PAIN POINT.** Down from 148 / 124 on 6/07, but the decline is matcher-driven, not schema-driven — the sharpening has still never landed (description unchanged for three weeks; see Diagnostic summary). The 5/31 audit found ~42 of the primary kids genuinely about AI-revenue disclosure (SEC concept release, audited monthly-revenue + WAU cadence, 10-Q footnote breakouts, AI-accelerator S-1 / IPO cohort, per-token-margin reporting) and the rest off-topic (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, CISA agentic-AI guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes, physical-AI procurement). Even at 90 primary the theme is still by far the largest in any scope and still ~48 above the genuine-disclosure core. Root cause is unchanged and structural: this theme's keyword set is the broadest of the 8 business themes, so the IDF tournament defaults to it whenever a business-scope prediction has no closer match. The fix is the description sharpening in recommendation 1 — re-emitted unchanged because it never landed.
- `business.inference_server_supply_chain` (80 children, 24 primary, 56 secondary) — primary core coherent (inference-server CVE class, CISA AI-Infra KEV sub-catalog, signed-checkpoint loading, NIST non-human-identity profile, agent-skills threat sub-matrix). Multi-attach inflation is `export.py`-driven. Leave as-is.
- `tech.model_supply_chain` (101 children, 48 primary) — strong growth this week and now the largest tech theme by both measures. Primary core coherent around model signing / provenance / loader-verification (the loader-verification keyword set widened on 5/24 is matching well). The high child count is secondary-attach inflation, not drift. Leave as-is.
- `tech.ai_macro_capital_markets` (62 children, 34 primary) — primary core uniform around macro / capital-markets dynamics. Leave as-is.
- `tech.frontier_model_regulatory_board` (29 children, 16 primary) — primary core coherent (FMRB executive order, AISI cyber-eval reciprocity ladder, allied procurement mirror clauses, allied AI-safety-institute mutual recognition). No drift. Leave as-is.
- `business.compute_capex_strategy` (67 children, 23 primary) — newly well-populated; primary core coherent around multi-year compute-capex commitments, GW-scale buildouts, forward-supply 8-K cadence. The description (sharpened 5/24, commit `e5e0a79`) is matching as intended now that fresh capex predictions arrived. Leave as-is.

## Theme candidates

No candidate label reaches the ≥ 3-hit raw-label bar (max within-scope hit count = 1). Manual topical clustering of the 38 pending rows surfaces the following groups, all already covered by existing themes:

- AI-Revenue / SEC-disclosure cluster — ~8 rows across both scopes (SEC AI-accelerator IPO Staff Legal Bulletin, SEC AI-accelerator S-1 staff guidance, SEC AI-compute-prebuy concept release, AI-accelerator S-1 per-counterparty risk-factor table, frontier-lab chip-supply warrant S-1 standard, Anthropic confidential S-1, AI-accelerator IPO cohort second >$10B deal, AGI-clause unwind). Covered by `business.ai_revenue_disclosure` (and `tech.ai_macro_capital_markets` on the tech side). These are exactly the genuine-disclosure predictions; once recommendation 1 sharpens the theme, they should match more cleanly rather than tie with off-topic kids.
- FMRB / allied cyber-eval reciprocity cluster — 4 rows (FMRB executive order ×3 wordings, OECD GENAI cyber-eval reciprocity Recommendation, allied procurement mirror clause). Covered by the live `tech.frontier_model_regulatory_board`.
- Frontier-lab voluntary-governance / safety-framework convergence — 2 rows, both tech, both Q2 2027 ("Frontier-lab governance frameworks converge on shared quantitative-harm thresholds", "Frontier-lab safety frameworks align on catastrophic-risk disclosure"). These are voluntary lab-side framework convergence, conceptually distinct from the regulatory-board / procurement-gate mechanics the FMRB theme centers on. Down from 3 loosely-related rows on 6/07 to 2 tightly-worded rows now — still below the bar but the wording has tightened. Log-only watch (see recommendation 3); if a third or fourth tightly-worded lab-governance row appears, prefer widening the FMRB description over adding a theme.
- Loader-verification default cluster — 4 rows (training-to-tenant signed default, unified loader-verification model, GGUF signed-card upload gate + SSTI scan, sandboxed-skill verification harness). Covered by `tech.model_supply_chain` (its description was widened to include loader-verification keywords on 5/24). Stale candidates.
- Local / edge-LLM recipe cluster — 4 rows (27B Dense + 1M context default local recipe, Bonsai 8B 1-bit native training default, local-LLM workstation three-tier signed-load, local-first coding agents cloud-overflow default). Split across `tech.one_bit_edge_llm`, `tech.local_inference_runtime`, and `business.cloud_vs_local_distribution`.
- Physical-AI industrial cluster — 2 rows (physical-AI league tables top-five OEM humanoids, robot-control training as per-robot cloud SaaS). Covered by `tech.physical_ai_robotics`.
- Hyperscaler / compute-coupling cluster — 3 rows (frontier-AI cloud three-cloud training oligopoly, hyperscaler-AI-lab capital coupling lock-in, multi-cloud frontier-model SLA standardization). Covered by `business.hyperscaler_frontier_lab_alliance` and `business.compute_capex_strategy`.
- Open-weight / coding-model benchmark cluster — 5 rows (Kimi K2.7-Code SWE-bench Verified, MiniMax M3 SWE-Bench Pro rerun, US open-weights >54 Artificial Analysis Index, agent-registry-MCP convergence, governed agent-ontology moat). Covered by `business.open_weight_vs_proprietary`, `tech.agent_registry_architecture`, and `tech.agent_control_plane`.
- Sub-threshold scattered rows (≤ 1 each): AI-accelerator XPU $20B/quarter run-rate, AI-accelerator equity re-rate on ASIC print, capital-markets reprice agent-displaced SaaS, AI-assisted vuln triage federal baseline, DOJ Apple-Siri Gemini search-remedies, cross-orchestrator interop matrix.

**No new-theme proposal warranted this week.** Every cluster at or above the 3-row bar is already covered by an existing theme. The only emerging cluster worth tracking is frontier-lab voluntary governance/safety-framework convergence, and at 2 rows it is below the bar and better handled (if it grows) by widening the existing FMRB theme than by adding a theme.

## Category-level notes

Category dominance (≥ 50 %): one — `business.capital-supply-chain` at **70.2 %** (down from 76.0 % on 6/07; the climb 52.2 % → 68.9 % → 74.0 % → 76.0 % finally reversed to 70.2 %). Still the only category over threshold and still the highest in the project. `tech.security` 39.2 % (now the top tech category, below threshold). `tech.infrastructure` 28.8 %. `tech.standards` 16.3 %. `business.regulation-compliance` 16.1 %. The business-side imbalance eased this week because the capex-strategy revival split the capital-supply-chain load across two themes instead of one, but the category still has no theme-level escape valve — every business theme except the two capital-supply-chain themes sits in a different, small-population category. The durable structural fix is a new category, which per §2.3 requires its own design discussion (recommendation 3).

## Recommended actions

### Action 1: Sharpen `business.ai_revenue_disclosure` description (re-emitted — still never landed after 3 weeks)

This is the same vetted sharpening proposed on 5/31 and 6/07, re-issued because the schema edit has never been applied (latest schema commit is still `e5e0a79`, 5/24; the `Big-3` anchor is confirmed present in the 5/31, 6/07, and 6/14 pre-review snapshot schemas). The theme over-fits on broad anchor-name tokens ("frontier", "hyperscaler", "AI-accelerator", "AI-services", "Big-3", "audited", bare company names) that let it win the IDF tournament on the off-topic share of its 90 primary kids that are not about AI-revenue disclosure (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, agentic-AI deployment guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes). The rewrite tightens the keyword set to disclosure mechanics only and explicitly drops the bare company-name and bare-category anchors that pull unrelated predictions in. Expected effect: off-topic primary kids re-route to other themes (or to `theme_candidates` if no other theme matches well), restoring the primary core toward the ~42-prediction genuine-disclosure cluster and locking in this week's organic relief of `business.capital-supply-chain` dominance so it does not reverse on a quiet news week. This is a sharpening, not a widening — total token count goes down by intent. Low-risk: it narrows one over-broad theme on a description swap; predictions that fall out are diagnostic signal for the §2.3 category carve-out conversation.

```action
{
  "kind": "rewrite-description",
  "theme_id": "business.ai_revenue_disclosure",
  "new_description_en": "The 2026 rewrite of how AI revenue is reported to capital markets, focused on disclosure-mechanic vocabulary: SEC AI-revenue concept release; Corporation Finance staff guidance and Staff Legal Bulletins on AI-accelerator IPO disclosure; audited monthly-revenue and WAU disclosure cadence as a recurring reporting primitive; per-token-margin disclosure as a recurring reporting primitive; 10-Q segment footnote breakouts as a disclosure mechanic (AI-services run-rate, AI-accelerator segment footnote, AI-business KPI footnote); AI-accelerator IPO and S-1 disclosure cohort mechanics; per-counterparty risk-factor tables in S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language in lab-hyperscaler contracts; AI-accelerator vendor forward-supply 8-K cadence as disclosure primitive; Cerebras / Tenstorrent / Anthropic IPO pricing and post-IPO disclosure mechanics specifically as filings. (Macro and capital-markets regime dynamics around these disclosures are covered by tech.ai_macro_capital_markets, not here. Compute-capex commitments are covered by business.compute_capex_strategy. Hyperscaler-lab partnership structure is covered by business.hyperscaler_frontier_lab_alliance.)",
  "new_description_ja": "2026年のAI売上の資本市場開示再構築、開示メカニクスの語彙に焦点: SEC AI-revenue concept release; AI-accelerator IPO 開示に関する Corporation Finance スタッフガイダンスおよび Staff Legal Bulletins; 反復報告プリミティブとしての監査済み月次売上 + WAU 開示ケイデンス; 反復報告プリミティブとしての per-token margin 開示; 開示メカニクスとしての 10-Q セグメント脚注ブレイクアウト (AI-services ランレート、AI-accelerator セグメント脚注、AI-business KPI 脚注); AI-accelerator IPO/S-1 開示コホートメカニクス; S-1 提出書類のカウンターパーティ別リスク要因表; ハイパースケーラー・アンカー新株予約権-持分開示文言; ラボ-ハイパースケーラー契約の AGI 条項 / 能力アテステーション開示文言; 開示プリミティブとしての AI-accelerator ベンダー先渡し供給 8-K ケイデンス; Cerebras / Tenstorrent / Anthropic の IPO 値付けおよび IPO 後開示メカニクス (具体的に提出書類として)。(これらの開示を取り巻くマクロおよび資本市場レジームのダイナミクスは tech.ai_macro_capital_markets 側でカバー。計算 capex コミットメントは business.compute_capex_strategy 側。ハイパースケーラー-ラボ パートナーシップ構造は business.hyperscaler_frontier_lab_alliance 側。)",
  "new_description_es": "La reescritura de 2026 sobre cómo se reporta el ingreso de IA a los mercados de capital, enfocada en vocabulario de mecánicas de divulgación: SEC AI-revenue concept release; staff guidance de Corporation Finance y Staff Legal Bulletins sobre la divulgación en IPOs de AI-accelerator; cadencia de divulgación de ingresos auditados mensuales + WAU como primitiva de reporte recurrente; divulgación de per-token margin como primitiva de reporte recurrente; breakouts de notas a pie en segmentos 10-Q como mecánica de divulgación (run-rate de AI-services, nota a pie de segmento de AI-accelerator, nota a pie de KPI de AI-business); mecánicas de la cohorte de divulgación de IPO y S-1 de AI-accelerator; tablas de factores de riesgo por contraparte en presentaciones S-1; lenguaje de divulgación de warrants-equity de anclaje de hyperscaler; lenguaje de divulgación de cláusulas AGI / atestación de capacidad en contratos lab-hyperscaler; cadencia de oferta forward 8-K de proveedores de AI-accelerator como primitiva de divulgación; mecánicas de pricing de IPO y divulgación post-IPO específicamente como presentaciones de Cerebras / Tenstorrent / Anthropic. (Las dinámicas de régimen macro y de mercados de capital alrededor de estas divulgaciones las cubre tech.ai_macro_capital_markets, no este tema. Los compromisos de capex de cómputo los cubre business.compute_capex_strategy. La estructura de partnerships hyperscaler-lab la cubre business.hyperscaler_frontier_lab_alliance.)",
  "new_description_fil": "Ang 2026 rewrite kung paano iniuulat ang kita ng AI sa capital markets, nakatuon sa vocabulary ng disclosure mechanics: SEC AI-revenue concept release; staff guidance ng Corporation Finance at mga Staff Legal Bulletin tungkol sa AI-accelerator IPO disclosure; cadence ng audited monthly-revenue at WAU disclosure bilang recurring reporting primitive; per-token-margin disclosure bilang recurring reporting primitive; 10-Q segment footnote breakouts bilang disclosure mechanic (run-rate ng AI-services, AI-accelerator segment footnote, AI-business KPI footnote); mga mechanic ng AI-accelerator IPO at S-1 disclosure cohort; mga per-counterparty risk-factor table sa S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language sa mga lab-hyperscaler contract; AI-accelerator vendor forward-supply 8-K cadence bilang disclosure primitive; mechanic ng IPO pricing at post-IPO disclosure ng Cerebras / Tenstorrent / Anthropic partikular bilang mga filing. (Ang macro at capital-markets regime dynamics sa paligid ng mga disclosure na ito ay sakop ng tech.ai_macro_capital_markets, hindi nito. Ang compute-capex commitments ay sakop ng business.compute_capex_strategy. Ang hyperscaler-lab partnership structure ay sakop ng business.hyperscaler_frontier_lab_alliance.)"
}
```

### Action 2: Observation (no schema edit) — `business.compute_capex_strategy` underused watch CLOSED; two new thin-primary themes on watch

The 6/07 underused watch on `business.compute_capex_strategy` (flat at 4 children / 3 primary for three weeks, with a deferred merge-into-`business.ai_revenue_disclosure` decision) is closed this week: the theme jumped to 67 children / 23 primary as fresh compute-capex predictions matched it directly against its 5/24-sharpened description. It is no longer underused, no longer a merge candidate, and the deferred merge decision is moot. Two themes take its place on the thin-primary watch list, neither a merge candidate (both carve distinct keyword sub-spaces, both stay visible via healthy secondary-attach child counts): `business.cloud_vs_local_distribution` (19 children, 1 primary) and `business.ai_security_compliance_market` (20 children, 2 primary). No schema edit this week; re-measure both next Sunday and flag only if primary stays ≤ 1 / ≤ 2 respectively.

```action
{"kind": "log-only"}
```

### Action 3: Observation (no schema edit) — business-scope category carve-out still overdue; frontier-lab-governance cluster down to 2 rows

Two log-only items folded together. First: `business.capital-supply-chain` eased to **70.2 %** primary density this week (from 76.0 %), its first decline after four straight weeks of climbing, driven by the capex-strategy revival splitting the category's load across two themes. But 70.2 % is still well over the 50 % flag line and still the highest single-category dominance in the project. The improvement is matcher-driven and reversible; the durable fix is a new `business.capital-markets` (or equivalent) category that pulls `business.ai_revenue_disclosure` out of `business.capital-supply-chain`. Per §2.3 a new category requires its own design discussion and is out of scope for the Sunday `apply-schema-edit` flow. Flagged five weeks running; should be promoted to milestone-priority. Second: the frontier-lab voluntary-governance / safety-framework-convergence candidate cluster narrowed from 3 loosely-related rows (6/07) to 2 tightly-worded Q2-2027 rows (shared quantitative-harm thresholds, catastrophic-risk disclosure alignment) — still below the 3-hit promotion bar and conceptually distinct from the regulatory-board / procurement-gate mechanics `tech.frontier_model_regulatory_board` centers on. If it grows to 3-4 tightly-worded rows next week, prefer a description widening of the existing FMRB theme over a new theme. No action this week.

```action
{"kind": "log-only"}
```

### Action 4: Investigation (no schema edit) — `export.py` secondary-attach threshold + `theme_candidates` label normalisation

Ninth consecutive review flagging the multi-attach pattern; eighth flagging candidate-label uniqueness. Tech multi-attach 62.1 %, business 71.4 %, mix 88.6 % (mix eased from 90.1 %). On the candidate side: 38 pending rows, all at within-scope hit count 1; the recurring cluster groups (AI-Revenue/SEC ~8 rows, FMRB 4, Loader-Verification 4, local/edge-LLM 4) are largely stale candidates created the same day their target theme's schema edit landed, plus fresh single-row predictions from this past week. The candidate-extraction pass still appears to run against the keyword set in effect at ingest time and not re-evaluate after a later same-day schema edit, so these inflate the pending count without representing genuine new clusters. Both problems are engineering work outside `apply-schema-edit` scope — `app/src/export.py` secondary-attach threshold and the candidate-extraction label-normalisation + post-schema-edit re-evaluation pass. Deferred again.

```action
{"kind": "log-only"}
```

## Why this rotation

The headline this week is twofold. First, the diagnosed `business.ai_revenue_disclosure` over-fit eased on the metrics — primary kids 124 → 90, `business.capital-supply-chain` dominance 76.0 % → 70.2 % (first decline in five weeks), and the previously-starved `business.compute_capex_strategy` revived from 3 to 23 primary. Second, and crucially, none of that came from a schema edit: the latest schema commit is still `e5e0a79` from 5/24, and the `business.ai_revenue_disclosure` sharpening proposed on 5/31 and 6/07 has still never landed (the broad `Big-3` anchor is confirmed present in three consecutive pre-review snapshots). The improvement is organic matcher re-tournamenting against fresh predictions and is reversible. The one recommended schema action this Sunday re-emits the 5/31/6/07 sharpening unchanged so the relief is locked into the schema; everything else — the still-overdue business-side category carve-out, the now-resolved capex-strategy watch, the two new thin-primary themes, the multi-attach / candidate-normalisation pipeline work, and the shrinking frontier-lab-governance cluster — is logged as observation, consistent with the conservative posture for an unattended `--mode auto` apply.

## Deferred for next week

- **`business.ai_revenue_disclosure` post-sharpen state.** If recommendation 1 finally lands, next Sunday should show primary kids drop from 90 toward the 40-50 range, off-topic predictions re-route or land in `theme_candidates`, and `business.capital-supply-chain` density fall from 70.2 % toward 50-55 %. If the primary count barely moves after the sharpening lands, the matcher is more keyword-promiscuous than expected and the proper fix escalates to the category-level intervention.
- **Business-scope category carve-out** (`business.capital-markets` or equivalent). Per §2.3, requires its own design discussion. Flagged five weeks running; promote to milestone-priority and stop deferring.
- **`business.cloud_vs_local_distribution`** (1 primary) and **`business.ai_security_compliance_market`** (2 primary, 0 in mix). Watch; flag only if primary stays ≤ 1 / ≤ 2 next week. Neither is a merge candidate — the sub-spaces differ.
- **Frontier-lab voluntary-governance cluster.** Currently 2 tightly-worded candidate rows. If it grows to 3-4 rows, widen the `tech.frontier_model_regulatory_board` description rather than add a theme.
- **Pipeline-level fixes** (export.py secondary-attach threshold, candidate-extraction label normalisation + post-schema-edit re-evaluation). Carry forward — engineering work outside `apply-schema-edit` scope.

---
