# Theme review — week ending 2026-07-12

Mode: routine Sunday rotation (5_weekly_theme_review). Diagnostic + advisory pass over the current schema. Inputs: `docs/data/graph-{tech,business,mix}.json` (post-update_pages snapshot, generated 2026-07-12), `theme_candidates` table (read-only, 53 pending rows), `app/src/schema.sql`.

## Diagnostic summary

Total themes: tech 10, business 8, mix 18 (unchanged). Total predictions: tech 223, business 246, mix 261. Pending `theme_candidates`: 53 rows (up from 49 on 7/05), all `candidate_reason='no_keyword_match'`, none carrying a `nearest_theme_id`. Max within-scope raw-label hit count = 1 across all 53 labels (each label is a distinct one-off prediction string). Twelfth consecutive week the `≥ 3 distinct-day` promotion bar cannot fire on raw labels (5/24, 5/31, 6/07, 6/14, 6/21, 6/28, 7/05, this week).

**The headline this week has flipped from bad to actionable: the `apply-schema-edit` parser bug that turned this step into a multi-week no-op has been identified, fixed, and is now live — so THIS Sunday's `apply-schema-edit --mode auto` should FINALLY land the two vetted edits that have never reached the schema.** Direct read of the live schema confirms both are still unapplied as of this write: `business.ai_revenue_disclosure` at `app/src/schema.sql:1234` still carries every broad anchor token the sharpening was meant to drop — "foundation labs (OpenAI, Anthropic, xAI)", "Big-3 hyperscaler AI-services", "Microsoft Azure AI Services run-rate", "Alphabet AI Services segment", "AMD AI-accelerator revenue" — present in both the canonical INSERT (line 1234) and the locale UPDATE block (lines 1556–1563). Separately, `business.cloud_vs_local_distribution` at `app/src/schema.sql:1213` still carries its original short/narrow description, and its locale UPDATE block (lines 1493–1497) sets labels only with no `description_*` fields, confirming the widened distribution-mechanic version emitted as Action 2 on 6/28 never landed. So the disclosure sharpening is now **seven** straight Sundays unapplied (5/31, 6/07, 6/14, 6/21, 6/28, 7/05, this week) and the cloud-vs-local widen is **two** straight Sundays unapplied (7/05, this week) since its 6/28 emission. Unlike prior weeks, the root cause is now known and closed: it was the `apply-schema-edit` fenced-block parser, not a proposal-format fault. Both blocks below are re-emitted verbatim so the now-fixed pipeline can apply them; recommendation 3 asks the parent to verify they actually land this Sunday.

This week the matcher re-ran against the **unchanged** schema and `business.ai_revenue_disclosure` primary kids rose **118 → 124** (child_ids 197 → 215), continuing the multi-week climb. `business.capital-supply-chain` category dominance ticked down for the first time in weeks to **69.5 %** (from 71.1 %) — but only because the business pool grew faster (225 → 246 predictions, +21) than its two capital-supply-chain themes (160 → 171 primary, +11); the disclosure theme's own primary core kept climbing in absolute terms. It remains the only category over the 50 % flag line and still the highest single-category dominance in the project. `business.compute_capex_strategy` grew organically again (42 → 47 primary, child_ids 107 → 125) but, as in every prior week, did not pull load off the disclosure theme — the category re-concentrated rather than balanced. At 124 primary the disclosure theme is by a wide margin the largest in any scope (next-largest primary core anywhere is `tech.model_supply_chain` at 70), its description is still the broadest of the 8 business themes, and the relief proposed seven Sundays running has never been locked into the schema. Recommendation 1 below re-emits the same vetted sharpening, unchanged.

### Tech scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| Model Supply Chain (`tech.model_supply_chain`) | tech.security | 156 | 70 |
| AI Macro & Capital Markets (`tech.ai_macro_capital_markets`) | tech.infrastructure | 88 | 38 |
| Agent Runtime Security (`tech.agent_runtime_security`) | tech.security | 79 | 22 |
| Agent Control Plane (`tech.agent_control_plane`) | tech.agents | 41 | 22 |
| Local Inference Runtime (`tech.local_inference_runtime`) | tech.inference-runtime | 28 | 19 |
| Frontier Model Regulatory Board (`tech.frontier_model_regulatory_board`) | tech.standards | 35 | 16 |
| Physical AI / Robotics (`tech.physical_ai_robotics`) | tech.infrastructure | 16 | 12 |
| Agent Registry Architecture (`tech.agent_registry_architecture`) | tech.standards | 38 | 10 |
| AI Chip Architecture (`tech.ai_chip_architecture`) | tech.infrastructure | 20 | 7 |
| 1-bit / Edge LLM (`tech.one_bit_edge_llm`) | tech.models | 30 | 7 |

Tech category density (primary attach via prediction `category_id`, 223 distinct predictions): tech.security 92 / 223 = 41.3 %, tech.infrastructure 57 / 223 = 25.6 %, tech.standards 26 / 223 = 11.7 %, tech.agents 22 / 223 = 9.9 %, tech.inference-runtime 19 / 223 = 8.5 %, tech.models 7 / 223 = 3.1 %. No category over 50 %. tech.security held its top-tech position (model-supply-chain grew to 156 / 70), but both it and tech.infrastructure sit comfortably below threshold and the FMRB carve-out continues to hold tech.standards balanced. Growth was broad this week (agent_runtime_security 69/18 → 79/22, agent_control_plane 35/18 → 41/22, one_bit_edge_llm 25/7 → 30/7) rather than concentrated in any one theme. Tech taxonomy remains healthy.

### Business scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| AI-Revenue Disclosure Rewrite (`business.ai_revenue_disclosure`) | business.capital-supply-chain | 215 | 124 |
| Inference Server Supply Chain (`business.inference_server_supply_chain`) | business.regulation-compliance | 125 | 33 |
| Compute Capex Strategy (`business.compute_capex_strategy`) | business.capital-supply-chain | 125 | 47 |
| Developer Toolchain Platformization (`business.developer_platformization`) | business.enterprise-adoption | 63 | 15 |
| Hyperscaler × Frontier Lab Alliance (`business.hyperscaler_frontier_lab_alliance`) | business.market-structure | 64 | 5 |
| Open Weight vs Proprietary AI (`business.open_weight_vs_proprietary`) | business.competition | 58 | 19 |
| Cloud vs Local AI Distribution (`business.cloud_vs_local_distribution`) | business.distribution | 37 | 1 |
| AI Security Compliance Market (`business.ai_security_compliance_market`) | business.regulation-compliance | 34 | 2 |

Business category density (primary attach, 246 distinct predictions): **business.capital-supply-chain 171 / 246 = 69.5 %** (down from 71.1 %), business.regulation-compliance 35 / 246 = 14.2 %, business.competition 19 / 246 = 7.7 %, business.enterprise-adoption 15 / 246 = 6.1 %, business.market-structure 5 / 246 = 2.0 %, business.distribution 1 / 246 = 0.4 %. Single-category dominance on business dipped for the first time in weeks but stays at the top of the project and remains the only category over the 50 % flag line. The prior-week read holds: `business.compute_capex_strategy` grew its own primary core (42 → 47) but `business.ai_revenue_disclosure` grew alongside it (118 → 124), so the category stays concentrated rather than balanced — the modest density dip is pool growth elsewhere, not relief on the disclosure theme. The structural fix (a new category to pull `business.ai_revenue_disclosure` out of `business.capital-supply-chain`) remains the durable answer; see recommendation 3.

### Mix scope

Rolls up tech + business; same theme + category shape as above, with primary re-tournamented across the combined 18-theme pool. Total unique predictions = 261. In the mix pool, several business themes shed nearly all their primary kids to better-matching tech themes — `business.ai_revenue_disclosure` drops to 17 primary (from 124 in business-only), `business.compute_capex_strategy` and `business.inference_server_supply_chain` shed most of theirs, and the thin business themes (`business.cloud_vs_local_distribution`, `business.ai_security_compliance_market`) fall to 0–1 primary — confirming these clusters are tech-flavored predictions parked in the business pool only because no tech theme competes in business-only mode. Mix category density is well-distributed: top is tech.security at 35.2 %, no category over 50 %.

### Multi-attach pressure (secondary-attach path in `app/src/export.py`)

The broad-context-prediction multi-attach pattern flagged every week since 4/27 persists — thirteenth consecutive week. Per the standing instruction to avoid re-parsing the 25MB+ `graph-*.json` files, the exact per-bucket histogram was not recomputed this week; the pattern is unchanged in shape (mix scope remains the most inflated, > 90 % multi-attach, as the prediction pool grew again to 261). This is an `export.py` secondary-attach-threshold property, not a schema fault; carried forward as an engineering item (recommendation 4).

## Empty / underused themes

No theme is empty (child/prediction count 0 or 1 on `child_ids`). The two lowest-primary themes:

- **`business.cloud_vs_local_distribution` — 37 children, 1 primary. FIFTH CONSECUTIVE WEEK AT 1 PRIMARY.** Healthy child count (37, up from 34) keeps it visible on the dashboard via secondary attach, but its primary core is the thinnest of any theme and stuck at 1 across 6/14, 6/21, 6/28, 7/05, and this week. Critically, the keyword expansion approved on 6/28 (Action 2) to widen this theme's description with distribution-mechanic vocabulary **still never landed** — `app/src/schema.sql:1213` shows the original short description and the locale UPDATE block (lines 1493–1497) sets labels only. So the "primary stuck at 1" reading is still confounded: the corrective widening has not actually been tested yet. It carves a genuinely distinct sub-space (on-device vs hosted distribution, cloud-overflow inversion, channel shifts, hosted-API vs open-weight-download) that does not overlap any other business theme's keyword set, so it is **not** a merge candidate; it simply wins few IDF tournaments on its still-narrow keyword set. Recommendation 2 re-emits the 6/28 widening unchanged so it finally lands now that the parser is fixed. The merge-to-`business.open_weight_vs_proprietary` escalation stays deferred until the widen has run for at least one week and primary is re-measured against the widened description.
- **`business.ai_security_compliance_market` — 34 children, 2 primary (0 primary in mix scope).** Unchanged posture from 7/05 (was 28 / 2). Healthy child count keeps it visible; its primary core stays thin because it competes against `business.inference_server_supply_chain` (same `business.regulation-compliance` category, 33 primary) for regulation/compliance predictions. Still not a merge candidate — the two themes carve genuinely different sub-spaces (compliance-market / spend vs supply-chain-CVE-governance) and the keyword sets are distinct. Log-only watch; the 6/28 trigger was "flag only if primary stays ≤ 2", and it is unchanged at 2. Deferred again this week to keep the Sunday apply run focused on landing the two long-pending rewrites; with the parser now fixed and those blocks expected to clear, next Sunday is the right time to prioritise a keyword expansion here if it is still ≤ 2.

The prior-week close on `business.compute_capex_strategy` holds: it grew again this week to 125 children / 47 primary on its 5/24-sharpened description and is firmly a healthy, well-populated theme. No regression there.

## Overpopulated themes

Several themes exceed the ≥ 6-prediction threshold; all are coherent except the one persistent structural concern:

- **`business.ai_revenue_disclosure` — 215 children, 124 primary. PERSISTENT, STILL-UNRESOLVED PAIN POINT.** Up from 197 / 118 on 7/05 — the sharpening has still never landed (description unchanged for seven weeks; see Diagnostic summary). The 5/31 audit found ~42 of the primary kids genuinely about AI-revenue disclosure (SEC concept release, audited monthly-revenue + WAU cadence, 10-Q footnote breakouts, AI-accelerator S-1 / IPO cohort, per-token-margin reporting) and the rest off-topic (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, CISA agentic-AI guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes, physical-AI procurement). At 124 primary the theme is now ~82 above the genuine-disclosure core and at its widest spread to date. Root cause is unchanged and structural: this theme's keyword set is the broadest of the 8 business themes, so the IDF tournament defaults to it whenever a business-scope prediction has no closer match. The fix is the description sharpening in recommendation 1 — re-emitted unchanged because it never landed, and now finally landable with the parser fixed.
- `business.inference_server_supply_chain` (125 children, 33 primary) — primary core coherent (inference-server CVE class, CISA AI-Infra KEV sub-catalog, signed-checkpoint loading, NIST non-human-identity profile, agent-skills threat sub-matrix). Multi-attach inflation is `export.py`-driven. Leave as-is.
- `tech.model_supply_chain` (156 children, 70 primary) — continued strong growth this week and still the largest tech theme by both measures. Primary core coherent around model signing / provenance / loader-verification (the loader-verification keyword set widened on 5/24 is matching well). The high child count is secondary-attach inflation, not topical drift, and the primary core does not split into a cleanly separable second cluster — it is one coherent supply-chain-security story — so no split is warranted per the "split only when children genuinely cluster" rule. Leave as-is.
- `business.compute_capex_strategy` (125 children, 47 primary) — continues to grow on its 5/24-sharpened description (commit `e5e0a79`); primary core coherent around multi-year compute-capex commitments, GW-scale buildouts, forward-supply 8-K cadence. Leave as-is.
- `tech.ai_macro_capital_markets` (88 children, 38 primary) — primary core uniform around macro / capital-markets dynamics. Leave as-is.
- `tech.frontier_model_regulatory_board` (35 children, 16 primary) — primary core coherent (FMRB executive order, AISI cyber-eval reciprocity ladder, allied procurement mirror clauses, allied AI-safety-institute mutual recognition). No drift. Leave as-is.

## Theme candidates

No candidate label reaches the ≥ 3-hit raw-label bar (max within-scope hit count = 1). Manual topical clustering of the 53 pending rows surfaces the following groups, all already covered by existing themes:

- AI-Revenue / SEC-disclosure / AI-accelerator-IPO cluster — the largest group across both scopes (SEC AI-compute-prebuy disclosure concept release, SEC Corp Fin AI-accelerator S-1 staff guidance, SEC Corp Fin AI-accelerator IPO Staff Legal Bulletin, AI-accelerator S-1 per-counterparty risk-factor table, frontier-lab chip-supply warrant S-1 disclosure standard, Anthropic confidential S-1, AI-accelerator IPO cohort second >$10B deal, custom AI-accelerator XPU $20B/quarter run-rate, AI-accelerator equities re-rate on next ASIC print, OpenAI public-listing step with US-government stake, OpenAI equity-coupled HBM allocation, capital markets reprice agent-displaced SaaS). Covered by `business.ai_revenue_disclosure` (and `tech.ai_macro_capital_markets` / `tech.ai_chip_architecture` on the tech side). These are exactly the genuine-disclosure predictions; once recommendation 1 sharpens the disclosure theme, they should match more cleanly rather than tie with off-topic kids. No new theme warranted — this is keyword coverage, not an unhomed cluster.
- FMRB / allied cyber-eval reciprocity cluster — several rows (FMRB executive order across wordings incl. "Frontier Model Review Board", "allied procurement mirror", "allied cyber-eval reciprocity ladder"; OECD GENAI cyber-eval reciprocity attestation Recommendation). Covered by the live `tech.frontier_model_regulatory_board`.
- Frontier-lab voluntary-governance / safety-framework convergence — 2 rows, both tech, both Q2 2027 ("Frontier-lab governance frameworks converge on shared quantitative-harm thresholds", "Frontier-lab safety frameworks align on catastrophic-risk disclosure"). These are voluntary lab-side framework convergence, conceptually distinct from the regulatory-board / procurement-gate mechanics the FMRB theme centers on. Flat at 2 tightly-worded rows from 6/14 through this week — still below the bar, wording unchanged. Log-only watch (see recommendation 3); if a third or fourth tightly-worded lab-governance row appears, prefer widening the FMRB description over adding a theme.
- Loader-verification default cluster — 3 rows (training-to-tenant signed default, unified loader-verification chain, GGUF signed-card upload gate + SSTI scan). Covered by `tech.model_supply_chain` (its description was widened to include loader-verification keywords on 5/24). Stale candidates.
- Local / edge-LLM recipe cluster — several rows (27B Dense + 1M context default local recipe, Bonsai 8B 1-bit native training default, local-LLM workstation three-tier signed-load, local-first coding agents cloud-overflow default). Split across `tech.one_bit_edge_llm`, `tech.local_inference_runtime`, and `business.cloud_vs_local_distribution`.
- Physical-AI industrial cluster — 2 rows (physical-AI league tables top-five OEM humanoids, robot-control training as per-robot cloud SaaS). Covered by `tech.physical_ai_robotics`.
- Hyperscaler / compute-coupling cluster — several rows (frontier-AI cloud three-cloud training oligopoly, hyperscaler-AI-lab capital coupling lock-in, multi-cloud frontier-model SLA standardization, AGI-clause unwind to peer contracts). Covered by `business.hyperscaler_frontier_lab_alliance` and `business.compute_capex_strategy`.
- Open-weight / coding-model benchmark cluster — several rows (Kimi K2.7-Code SWE-bench Verified, MiniMax M3 SWE-Bench Pro rerun, US open-weights >54 Artificial Analysis Index, agent-registry-MCP convergence, governed agent-ontology moat). Covered by `business.open_weight_vs_proprietary`, `tech.agent_registry_architecture`, and `tech.agent_control_plane`.
- Sub-threshold scattered rows (≤ 1 each): non-GPU AI-datacenter silicon standalone funding, AMD MI400 memory-bandwidth-over-FLOPS marketing, sovereign-AI national champion unicorn, autonomous-pentest named managed tier / enterprise-gov deal, Google DeepMind senior-researcher retention package, OpenSharing cross-org asset protocol, US export-control defensive-cyber carve-out, DOJ Apple-Siri Gemini search-remedies, AI-assisted vuln triage federal baseline, cross-orchestrator interop matrix, Unsloth agent-loader mitigation default, sandboxed-skill verification harness. The +4 net new pending rows since 7/05 are fresh single-row predictions from this past week, not a new coherent cluster.

**No new-theme proposal warranted this week.** Every cluster at or above the 3-row bar is already covered by an existing theme; the largest unmatched-candidate cluster (AI-accelerator IPO / S-1 / SEC disclosure) is a keyword-coverage gap against `business.ai_revenue_disclosure` and `tech.ai_chip_architecture`, not a genuinely unhomed cluster, so it is addressed by the recommendation-1 sharpening rather than an `add`. The only emerging cluster worth tracking is frontier-lab voluntary governance / safety-framework convergence, and at 2 rows it is below the bar and better handled (if it grows) by widening the existing FMRB theme.

## Category-level notes

Category dominance (≥ 50 %): one — `business.capital-supply-chain` at **69.5 %** (down from 71.1 % on 7/05; the multi-week track 68.9 % → 74.0 % → 76.0 % → 70.2 % → 71.4 % → 71.1 % → 71.1 % → 69.5 % has held in the low-70s and dipped this week only because the rest of the business pool grew faster). Still the only category over threshold and still the highest single-category dominance in the project. `tech.security` 41.3 % (the top tech category, below threshold). `tech.infrastructure` 25.6 %. `tech.standards` 11.7 %. `business.regulation-compliance` 14.2 %. The business-side imbalance is structural: both capital-supply-chain themes (`business.ai_revenue_disclosure` 124 primary, `business.compute_capex_strategy` 47 primary) grew this week, and every other business theme sits in a different, small-population category, so there is no theme-level escape valve. The durable structural fix is a new category, which per §2.3 requires its own design discussion (recommendation 3).

## Recommended actions

### Action 1: Sharpen `business.ai_revenue_disclosure` description (re-emitted — never landed after 7 weeks; parser now fixed so this is the week it should apply)

This is the same vetted sharpening proposed on 5/31, 6/07, 6/14, 6/21, 6/28, and 7/05, re-issued because the schema edit has never been applied — the six-week no-op was the `apply-schema-edit` fenced-block parser bug, now fixed and live, so this Sunday's `--mode auto` run should finally land it. The current `app/src/schema.sql:1234` still carries the broad "foundation labs (OpenAI, Anthropic, xAI)" / "Big-3 hyperscaler AI-services" / "Microsoft Azure AI Services" / "Alphabet AI Services" / "AMD AI-accelerator revenue" anchors (confirmed present in both the canonical INSERT and the locale UPDATE block, lines 1556–1563). The theme over-fits on those broad anchor-name tokens (plus "frontier", "hyperscaler", "AI-accelerator", "AI-services", "audited") that let it win the IDF tournament on the off-topic share of its 124 primary kids that are not about AI-revenue disclosure (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, agentic-AI deployment guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes). The rewrite tightens the keyword set to disclosure mechanics only and explicitly drops the bare company-name and bare-category anchors that pull unrelated predictions in. Expected effect: off-topic primary kids re-route to other themes (or to `theme_candidates` if no other theme matches well), restoring the primary core toward the ~42-prediction genuine-disclosure cluster and locking in the relief of `business.capital-supply-chain` dominance so it stops climbing week-to-week (90 → 124 → 90 → 102 → 109 → 118 → 124 over the last seven weeks is exactly the matcher noise the sharpening removes). This is a sharpening, not a widening — total token count goes down by intent. Low-risk: it narrows one over-broad theme on a description swap; predictions that fall out are diagnostic signal for the §2.3 category carve-out conversation.

```action
{
  "kind": "rewrite-description",
  "theme_id": "business.ai_revenue_disclosure",
  "new_description_en": "The 2026 rewrite of how AI revenue is reported to capital markets, focused on disclosure-mechanic vocabulary: SEC AI-revenue concept release; Corporation Finance staff guidance and Staff Legal Bulletins on AI-accelerator IPO disclosure; audited monthly-revenue and WAU disclosure cadence as a recurring reporting primitive; per-token-margin disclosure as a recurring reporting primitive; 10-Q segment footnote breakouts as a disclosure mechanic (AI-services run-rate, AI-accelerator segment footnote, AI-business KPI footnote); AI-accelerator IPO and S-1 disclosure cohort mechanics; per-counterparty risk-factor tables in S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language in lab-hyperscaler contracts; AI-accelerator vendor forward-supply 8-K cadence as disclosure primitive; Cerebras / Tenstorrent / Anthropic IPO pricing and post-IPO disclosure mechanics specifically as filings. (Macro and capital-markets regime dynamics around these disclosures are covered by tech.ai_macro_capital_markets, not here. Compute-capex commitments are covered by business.compute_capex_strategy. Hyperscaler-lab partnership structure is covered by business.hyperscaler_frontier_lab_alliance.)",
  "new_description_ja": "2026年のAI売上の資本市場開示再構築、開示メカニクスの語彙に焦点: SEC AI-revenue concept release; AI-accelerator IPO 開示に関する Corporation Finance スタッフガイダンスおよび Staff Legal Bulletins; 反復報告プリミティブとしての監査済み月次売上 + WAU 開示ケイデンス; 反復報告プリミティブとしての per-token margin 開示; 開示メカニクスとしての 10-Q セグメント脚注ブレイクアウト (AI-services ランレート、AI-accelerator セグメント脚注、AI-business KPI 脚注); AI-accelerator IPO/S-1 開示コホートメカニクス; S-1 提出書類のカウンターパーティ別リスク要因表; ハイパースケーラー・アンカー新株予約権-持分開示文言; ラボ-ハイパースケーラー契約の AGI 条項 / 能力アテステーション開示文言; 開示プリミティブとしての AI-accelerator ベンダー先渡し供給 8-K ケイデンス; Cerebras / Tenstorrent / Anthropic の IPO 値付けおよび IPO 後開示メカニクス (具体的に提出書類として)。(これらの開示を取り巻くマクロおよび資本市場レジームのダイナミクスは tech.ai_macro_capital_markets 側でカバー。計算 capex コミットメントは business.compute_capex_strategy 側。ハイパースケーラー-ラボ パートナーシップ構造は business.hyperscaler_frontier_lab_alliance 側。)",
  "new_description_es": "La reescritura de 2026 sobre cómo se reporta el ingreso de IA a los mercados de capital, enfocada en vocabulario de mecánicas de divulgación: SEC AI-revenue concept release; staff guidance de Corporation Finance y Staff Legal Bulletins sobre la divulgación en IPOs de AI-accelerator; cadencia de divulgación de ingresos auditados mensuales + WAU como primitiva de reporte recurrente; divulgación de per-token margin como primitiva de reporte recurrente; breakouts de notas a pie en segmentos 10-Q como mecánica de divulgación (run-rate de AI-services, nota a pie de segmento de AI-accelerator, nota a pie de KPI de AI-business); mecánicas de la cohorte de divulgación de IPO y S-1 de AI-accelerator; tablas de factores de riesgo por contraparte en presentaciones S-1; lenguaje de divulgación de warrants-equity de anclaje de hyperscaler; lenguaje de divulgación de cláusulas AGI / atestación de capacidad en contratos lab-hyperscaler; cadencia de oferta forward 8-K de proveedores de AI-accelerator como primitiva de divulgación; mecánicas de pricing de IPO y divulgación post-IPO específicamente como presentaciones de Cerebras / Tenstorrent / Anthropic. (Las dinámicas de régimen macro y de mercados de capital alrededor de estas divulgaciones las cubre tech.ai_macro_capital_markets, no este tema. Los compromisos de capex de cómputo los cubre business.compute_capex_strategy. La estructura de partnerships hyperscaler-lab la cubre business.hyperscaler_frontier_lab_alliance.)",
  "new_description_fil": "Ang 2026 rewrite kung paano iniuulat ang kita ng AI sa capital markets, nakatuon sa vocabulary ng disclosure mechanics: SEC AI-revenue concept release; staff guidance ng Corporation Finance at mga Staff Legal Bulletin tungkol sa AI-accelerator IPO disclosure; cadence ng audited monthly-revenue at WAU disclosure bilang recurring reporting primitive; per-token-margin disclosure bilang recurring reporting primitive; 10-Q segment footnote breakouts bilang disclosure mechanic (run-rate ng AI-services, AI-accelerator segment footnote, AI-business KPI footnote); mga mechanic ng AI-accelerator IPO at S-1 disclosure cohort; mga per-counterparty risk-factor table sa S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language sa mga lab-hyperscaler contract; AI-accelerator vendor forward-supply 8-K cadence bilang disclosure primitive; mechanic ng IPO pricing at post-IPO disclosure ng Cerebras / Tenstorrent / Anthropic partikular bilang mga filing. (Ang macro at capital-markets regime dynamics sa paligid ng mga disclosure na ito ay sakop ng tech.ai_macro_capital_markets, hindi nito. Ang compute-capex commitments ay sakop ng business.compute_capex_strategy. Ang hyperscaler-lab partnership structure ay sakop ng business.hyperscaler_frontier_lab_alliance.)"
}
```

### Action 2: Widen `business.cloud_vs_local_distribution` description (re-emitted — 6/28 widen never landed; theme still at 1 primary, now fifth week)

This is the same widening approved on 6/28 (Action 2) and re-emitted on 7/05, re-issued again because it never reached `app/src/schema.sql` — line 1213 still carries the original short description and the locale UPDATE block (lines 1493–1497) has no `description_*` fields at all. The theme's primary core has now sat at 1 for five consecutive weeks (6/14, 6/21, 6/28, 7/05, this week) while its child count grew 19 → 26 → 30 → 34 → 37 (it stays dashboard-visible only via secondary attach). Because the corrective widen has not actually been applied yet, the "stuck at 1" reading still cannot be attributed to a genuinely-too-thin sub-space. The theme carves a genuinely distinct sub-space — on-device vs hosted distribution, cloud-overflow inversion, channel shifts, hosted-API vs open-weight-download — so it is not a merge candidate; it simply wins almost no IDF tournaments because its keyword set is the narrowest of the 8 business themes and most distribution-flavored predictions also carry stronger capital / compliance signal. This rewrite widens the keyword set with the distribution-mechanic vocabulary the matcher currently lacks so the theme can win a fair share of genuinely distribution-flavored tournaments without poaching from the capital / compliance themes. This is a deliberate widening (scoped to distribution mechanics only), the mirror of recommendation 1's narrowing — low-risk because the added tokens are all distribution-specific and do not overlap the capital-supply-chain or regulation-compliance keyword sets. With the parser fix live it should finally apply this Sunday. Expected effect: primary core rises off 1 toward the handful of cloud-vs-local distribution predictions currently being absorbed by broader themes. If it lands and primary still stays at 1 next Sunday, the merge-into-`business.open_weight_vs_proprietary` escalation becomes the appropriate call.

```action
{
  "kind": "rewrite-description",
  "theme_id": "business.cloud_vs_local_distribution",
  "new_description_en": "How AI capability is distributed and delivered — local on-device inference versus hosted cloud frontier AI, and the channel shifts between them. Edge / on-device deployment as a distribution channel; cloud-overflow inversion (local-first with cloud burst); SMB and prosumer local-first adoption; hosted-API versus open-weight-download distribution-model shifts; per-seat local workstation distribution; privacy-driven and cost-driven local distribution choices; three-cloud versus single-vendor hosted distribution; local-first coding-agent distribution with cloud-overflow default; channel-shift economics between download, marketplace, and hosted API. (Open-weight versus proprietary licensing dynamics are covered by business.open_weight_vs_proprietary; local inference-runtime tooling is covered by tech.local_inference_runtime; this theme is about the distribution channel and delivery model, not the license or the runtime stack.)",
  "new_description_ja": "AI能力がどのように配布・提供されるか — ローカルなオンデバイス推論 対 ホスト型クラウド・フロンティアAI、およびその間のチャネルシフト。配布チャネルとしてのエッジ/オンデバイス展開; クラウドオーバーフロー反転 (ローカルファースト + クラウドバースト); SMB・プロシューマーのローカルファースト採用; ホスト型API 対 オープンウェイト・ダウンロードの配布モデルシフト; シート単位ローカルワークステーション配布; プライバシー駆動・コスト駆動のローカル配布選択; 三クラウド 対 単一ベンダーのホスト型配布; クラウドオーバーフローをデフォルトとするローカルファースト・コーディングエージェント配布; ダウンロード・マーケットプレイス・ホスト型API間のチャネルシフト経済性。(オープンウェイト 対 プロプライエタリのライセンス力学は business.open_weight_vs_proprietary 側; ローカル推論ランタイムのツーリングは tech.local_inference_runtime 側。本テーマは配布チャネルと提供モデルが対象であり、ライセンスやランタイムスタックではない。)",
  "new_description_es": "Cómo se distribuye y entrega la capacidad de IA — inferencia local en dispositivo frente a IA frontera alojada en la nube, y los cambios de canal entre ambas. Despliegue edge / en-dispositivo como canal de distribución; inversión de cloud-overflow (local-first con ráfaga a la nube); adopción local-first de PYMEs y prosumidores; cambios de modelo de distribución entre API alojada y descarga de open-weight; distribución por puesto en workstation local; elecciones de distribución local impulsadas por privacidad y por costo; distribución alojada de tres-nubes frente a un solo proveedor; distribución de agentes de código local-first con cloud-overflow por defecto; economía de cambio de canal entre descarga, marketplace y API alojada. (Las dinámicas de licencia open-weight frente a propietario las cubre business.open_weight_vs_proprietary; el tooling de runtime de inferencia local lo cubre tech.local_inference_runtime; este tema trata del canal de distribución y el modelo de entrega, no de la licencia ni del runtime.)",
  "new_description_fil": "Kung paano idinidistribute at inihahatid ang kakayahan ng AI — local na on-device inference kontra hosted cloud frontier AI, at ang mga channel shift sa pagitan nila. Edge / on-device deployment bilang distribution channel; cloud-overflow inversion (local-first na may cloud burst); local-first adoption ng SMB at prosumer; mga shift sa distribution model sa pagitan ng hosted-API at open-weight-download; per-seat local workstation distribution; privacy-driven at cost-driven na local distribution choices; three-cloud kontra single-vendor hosted distribution; local-first coding-agent distribution na may cloud-overflow default; channel-shift economics sa pagitan ng download, marketplace, at hosted API. (Ang open-weight kontra proprietary licensing dynamics ay sakop ng business.open_weight_vs_proprietary; ang local inference-runtime tooling ay sakop ng tech.local_inference_runtime; ang temang ito ay tungkol sa distribution channel at delivery model, hindi sa lisensya o runtime stack.)"
}
```

### Action 3: Observation (no schema edit) — apply-schema-edit parser bug fixed and live; business category carve-out still overdue; frontier-lab-governance cluster flat at 2

Three log-only items folded together for the parent's attention.

First and most important: **the `apply-schema-edit --mode auto` no-op has been root-caused and fixed.** The multi-week failure to land edits was the fenced-block parser inside `apply-schema-edit`, not a proposal-format fault (the blocks always validated and matched the §2.1 schema). That parser fix is now live, so this Sunday's apply run should finally apply Actions 1 and 2. Two independent rewrite-description blocks remain confirmed unapplied against the live schema this week — the disclosure sharpening (seven Sundays: 5/31, 6/07, 6/14, 6/21, 6/28, 7/05, this week) and the cloud-vs-local widen (two Sundays: 7/05, this week). `app/src/schema.sql:1234` still holds the broad disclosure anchors and `:1213` still holds the narrow cloud-vs-local description. Recommend the parent verify Step 8 actually applies both blocks this Sunday and inspect its exit log to confirm — if, despite the parser fix, either block still does not land, escalate to a direct engineering look rather than re-emitting an eighth time.

Second: `business.capital-supply-chain` is at **69.5 %** primary density, down slightly from 71.1 % but still the only category over the 50 % flag line and the highest single-category dominance in the project. Both capital-supply-chain themes grew this week (`business.ai_revenue_disclosure` 118 → 124, `business.compute_capex_strategy` 42 → 47), so the category stays concentrated rather than balanced — the dip is pool growth elsewhere, not relief. The durable fix is a new `business.capital-markets` (or equivalent) category that pulls `business.ai_revenue_disclosure` out of `business.capital-supply-chain`. Per §2.3 a new category requires its own design discussion and is out of scope for the Sunday `apply-schema-edit` flow. Flagged nine weeks running; should be promoted to milestone-priority and stop being deferred.

Third: the frontier-lab voluntary-governance / safety-framework-convergence candidate cluster stayed flat at 2 tightly-worded Q2-2027 rows (shared quantitative-harm thresholds, catastrophic-risk disclosure alignment) — still below the 3-hit promotion bar and conceptually distinct from the regulatory-board / procurement-gate mechanics `tech.frontier_model_regulatory_board` centers on. If it grows to 3-4 tightly-worded rows, prefer a description widening of the existing FMRB theme over a new theme. No action this week.

```action
{"kind": "log-only"}
```

### Action 4: Investigation (no schema edit) — `export.py` secondary-attach threshold + `theme_candidates` label normalisation; tech taxonomy healthy

Thirteenth consecutive review flagging the multi-attach pattern; twelfth flagging candidate-label uniqueness. The multi-attach shape is unchanged (mix scope most inflated at > 90 %, tech and business lower); the exact per-bucket histogram was intentionally not recomputed this week per the standing instruction to avoid re-parsing the 25MB+ `graph-*.json` files. On the candidate side: 53 pending rows (up from 49), all at within-scope hit count 1; the recurring cluster groups (AI-Revenue/SEC largest, FMRB, loader-verification, local/edge-LLM, hyperscaler-coupling, open-weight/coding-benchmark) are largely stale candidates created the same day their target theme's schema edit landed, plus fresh single-row predictions from this past week. The candidate-extraction pass still appears to run against the keyword set in effect at ingest time and not re-evaluate after a later same-day schema edit, so these inflate the pending count without representing genuine new clusters. Separately, the tech scope is healthy: no tech category over 50 % (top is tech.security at 41.3 %), `tech.model_supply_chain` is overpopulated (156 / 70) but coherent and non-splittable, and no tech theme is empty or underused. Both pipeline problems are engineering work outside `apply-schema-edit` scope — `app/src/export.py` secondary-attach threshold and the candidate-extraction label-normalisation + post-schema-edit re-evaluation pass. Deferred again.

```action
{"kind": "log-only"}
```

## Why this rotation

The headline this week is that the apply-pipeline root cause is finally known and closed: the `apply-schema-edit` fenced-block parser bug that made this step a multi-week no-op has been fixed and is now live, so this Sunday's `--mode auto` run should at last land the two vetted edits. Against the still-unchanged schema, `business.ai_revenue_disclosure` primary kids went 118 → 124 (child_ids 197 → 215) and `business.capital-supply-chain` dominance eased slightly to 69.5 % (pool growth elsewhere, not relief), while `app/src/schema.sql:1234` still carries the broad `foundation labs (OpenAI` / `Big-3` anchors and `:1213` still carries the pre-widen cloud-vs-local description. `business.compute_capex_strategy` grew healthily on its own (42 → 47 primary) but did not absorb load off the disclosure theme. This Sunday carries two schema actions, both verbatim re-emissions of already-vetted edits that never reached the schema: recommendation 1 re-emits the disclosure sharpening so the relief is finally locked in, and recommendation 2 re-emits the 6/28 `business.cloud_vs_local_distribution` widening. Everything else — the now-fixed apply-pipeline signal, the still-overdue business-side category carve-out, the `business.ai_security_compliance_market` thin-primary watch, the multi-attach / candidate-normalisation pipeline work, the healthy tech taxonomy, and the flat frontier-lab-governance cluster — is logged as observation. No new edits were introduced this week specifically to keep the Sunday apply run focused on landing the two prior edits now that the parser can finally apply them.

## Deferred for next week

- **`business.ai_revenue_disclosure` post-sharpen state.** If recommendation 1 finally lands this Sunday, next Sunday should show primary kids drop from 124 toward the 40-50 range, off-topic predictions re-route or land in `theme_candidates`, and `business.capital-supply-chain` density fall from 69.5 % toward 50-55 %. If the primary count barely moves after the sharpening lands, the matcher is more keyword-promiscuous than expected and the proper fix escalates to the category-level intervention.
- **`business.cloud_vs_local_distribution` post-widen state.** If recommendation 2 lands, next Sunday should show primary rise off 1 toward the handful of genuinely distribution-flavored predictions. If it stays at 1 even after the widening actually applies, the sub-space is too thin to sustain a standalone theme and a merge into `business.open_weight_vs_proprietary` should be reconsidered.
- **apply-schema-edit pipeline health.** The parser bug is fixed and live; this is the first Sunday the two blocks are expected to apply. Verify both actually land this run. If they still do not land despite the fix, the apply step needs a direct engineering look (is Step 8 running against the right proposal path? is it rolling back on this schema?) rather than another re-emission.
- **Business-scope category carve-out** (`business.capital-markets` or equivalent). Per §2.3, requires its own design discussion. Flagged nine weeks running; promote to milestone-priority and stop deferring.
- **`business.ai_security_compliance_market`** (2 primary, 0 in mix). Watch; prioritise a keyword expansion next Sunday if primary stays ≤ 2 now that the two long-pending rewrites are expected to clear.
- **Frontier-lab voluntary-governance cluster.** Currently 2 tightly-worded candidate rows, flat since 6/14. If it grows to 3-4 rows, widen the `tech.frontier_model_regulatory_board` description rather than add a theme.
- **Pipeline-level fixes** (export.py secondary-attach threshold, candidate-extraction label normalisation + post-schema-edit re-evaluation). Carry forward — engineering work outside `apply-schema-edit` scope.

---
