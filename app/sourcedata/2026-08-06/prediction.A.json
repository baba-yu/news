{
  "id": "prediction.98ae68245f0ead55",
  "scope_hint": "tech",
  "title": "LFM2.5-2.6B's under-2.5 GB claim gets a named context length by Q3 2027",
  "body": "Liquid AI published LFM2.5-2.6B on Tuesday, August 4: 2.6 billion parameters, pre-trained on roughly 34 trillion tokens, with the context window extended to 128K mid-training, and aimed squarely at agent work - planning, tool calls, multi-step tasks - that finishes on the device instead of round-tripping to a hosted service. Three numbers carry the pitch. The footprint is under 2.5 GB of memory. The context window is 128K. The tool-use score on ToolSandbox is 77.83, against 65.00 for Gemma 4 E4B-it and 52.40 for Gemma 4 E2B-it. Runtime support at release spans llama.cpp, MLX, vLLM, SGLang and ONNX, on transformers 5.0.0 or later. Every one of those figures is the vendor's own, published alongside the model rather than independently reproduced, and that is the caveat most readers will apply.\n\nIt is not the interesting caveat. The interesting one is that two of those three numbers describe different states of the same process, and the release never states them together. Under 2.5 GB is a figure about the model. 128K is a figure about how much of a session the model can hold. A tool-calling agent loop is the case where the two meet, and the release does not say where. A loop that runs long enough to be worth calling an agent has accumulated tool output, intermediate plans and its own transcript, and what that accumulation costs is what decides whether the loop is still resident late in a session rather than at the start of one. Nothing published with the model says what it costs at 8K, or at 32K, or at the 128K it advertises. It says under 2.5 GB, and separately it says 128K, and the reader is left to assume the first survives the second.\n\nThat gap matters more here than it would for a hosted model, because the entire argument for this release is a hardware ceiling. The company's own device measurements - 220 tokens per second on an Apple M5 Max, 113 tokens per second on an AMD Ryzen AI Max+ 395 - are throughput, and throughput is not footprint. The figure of close to 15,000 output tokens per second on a single NVIDIA H100 under high concurrency is a server number and answers a different deployment question again. A developer deciding whether an agent fits on a laptop, a handset or an embedded board is not asking how fast it runs. They are asking what it will be holding when the session is deep, on hardware where running out is a crash rather than an autoscale. The memory ceiling is the number this release chose to lead with, and it is the number it states with the least qualification.\n\nThe prediction is that the ceiling acquires a context length. By the end of Q3 2027, Liquid AI's own published material for LFM2.5-2.6B - the model card, the repository documentation, or a follow-up post from the company - states a memory figure together with the context length that figure holds at. A table with a memory number in one column and a context length in another settles it. So does a stated peak-memory figure for a session run at a named length. The direction the number moves is not the claim; that it is stated at all is.\n\nSeveral weaker things do not count, and each is worth naming because each is what a publisher under this pressure reaches for first. A repeat of the unqualified figure does not count, however prominently placed - that is the thing that already happened. A table of file sizes per quantisation level does not count, because a file on disk is not what a running loop is holding. A tokens-per-second figure measured at a named context length does not count on its own, because throughput at a length says nothing about footprint at that length. A configuration file from which a reader could derive the answer does not count, because the audience for a device-deployment claim is the developer choosing between models, not the one already committed enough to read the config. And a figure published for a different member of the family does not count; the claim under test is the one attached to this release.\n\nThe strongest objection is that a vendor is under no pressure to qualify a headline that is working. Practitioners running llama.cpp work this out for themselves, and asking a company to publish the number that makes its own ceiling look worse is asking it to argue against its own launch. The second objection is architectural, and it is the one that would make this prediction not merely unfulfilled but unnecessary: the release says nothing about how this model's memory scales as context fills, and if it scales unusually flatly then the unqualified ceiling is closer to honest than it looks and there is nothing to restate. The third is that attention goes where the competition is, and the competition here is the benchmark table - 77.83 against 65.00 and 52.40 is the comparison a rival answers, so the next numbers published in this space are more likely to be scores than footprints.\n\nThe honest failure mode is quieter than any of those. The figure may simply never appear, not because it is unflattering but because nobody asked for it, and a prediction that something goes unsaid reads as wrong when what actually happened is that nothing happened. This is a claim about what the publisher commits to in writing. Silence settles it against me.",
  "reasoning": {
    "because": "LFM2.5-2.6B ships an under-2.5 GB footprint and a 128K context as separate, unpaired numbers",
    "given": "A multi-step agent session accumulates context, and the release states no cost at any length",
    "so_that": "Liquid AI's own material states a memory figure for LFM2.5-2.6B beside the context length it holds at",
    "landing": "by Q3 2027; a memory figure beside a context length, not a bare ceiling",
    "plain_language": "The model is advertised at under 2.5 GB. No published number says what it needs while an agent task is actually running."
  },
  "summary": "Liquid AI's LFM2.5-2.6B is published at under 2.5 GB of memory and, separately, a 128K context window, with no figure stating what the model costs at a given context. A multi-step agent session accumulates context. Expect Liquid AI's own material to pair the two numbers by Q3 2027."
}
